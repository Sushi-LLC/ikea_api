# 🔐 Авторизация Swagger

## Описание

Swagger документация защищена авторизацией. Для доступа требуется войти как администратор.

## Дефолтный администратор

После выполнения `rails db:seed` создается системный администратор:

- **Username**: `admin`
- **Password**: `admin123` (или значение из `ADMIN_PASSWORD`)
- **Email**: `admin@ikea_api.local` (или значение из `ADMIN_EMAIL`)
- **Role**: `admin`

⚠️ **ВНИМАНИЕ**: В production обязательно измените пароль!

## Использование

### 1. Откройте Swagger

Перейдите по адресу: `http://localhost:3000/api-docs`

### 2. Автоматическое перенаправление

Если вы не авторизованы, вас автоматически перенаправит на форму входа: `/api-docs/login`

### 3. Вход

Введите учетные данные администратора и нажмите "Войти"

### 4. Доступ к Swagger

После успешного входа вы получите доступ к Swagger UI

### 5. Выход

Для выхода перейдите по адресу: `/api-docs/logout`

## Изменение пароля администратора

### Через переменные окружения

```bash
# В .env файле
ADMIN_PASSWORD=your_secure_password
ADMIN_EMAIL=your_email@example.com

# Затем выполните
rails db:seed
```

### Через Rails консоль

```bash
rails console

admin = User.find_by(username: 'admin')
admin.update(password: 'new_password', password_confirmation: 'new_password')
```

## Создание дополнительных администраторов

```bash
rails console

User.create!(
  username: 'new_admin',
  email: 'new_admin@example.com',
  password: 'secure_password',
  password_confirmation: 'secure_password',
  role: 'admin',
  is_active: true
)
```

## Безопасность

- Сессии хранятся в cookies
- Требуется роль `admin` для доступа
- CSRF защита включена для формы авторизации
- Пароли хешируются через bcrypt

# 🔧 Решение проблем с авторизацией Swagger

## Проблема: После входа не происходит перенаправление на Swagger

### Возможные причины:

1. **Сессия не сохраняется**
   - Проверьте, что cookies включены в браузере
   - Проверьте консоль браузера на наличие ошибок
   - Убедитесь, что секретный ключ установлен

2. **Middleware блокирует доступ**
   - Проверьте порядок middleware
   - Убедитесь, что SwaggerAuthMiddleware идет после Session middleware

3. **CSRF токен**
   - Убедитесь, что форма содержит CSRF токен
   - Проверьте, что protect_from_forgery включен

### Решение:

1. **Проверка сессии в консоли браузера:**
   ```javascript
   document.cookie
   // Должен содержать _ikea_api_session
   ```

2. **Проверка в Rails консоли:**
   ```ruby
   # Проверка пользователя
   User.find_by(username: 'admin')
   
   # Проверка middleware
   Rails.application.config.middleware.to_a.map(&:name)
   ```

3. **Очистка cookies и повторная попытка**

4. **Проверка логов:**
   ```bash
   tail -f log/development.log
   ```

### Альтернативное решение (если сессии не работают):

Можно использовать JWT токены вместо сессий для авторизации Swagger.
